# GitHub Pages Note

GitHub Pages can host only static frontend files. Nexora needs a FastAPI backend for chat, memory, uploads, search, and image generation, so GitHub Pages alone is not enough for the real AI app.

Use a full-stack host instead:

- Render
- Railway
- Fly.io
- Any Docker server

See `DEPLOYMENT.md` for the public website setup.
